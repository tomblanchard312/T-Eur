# tEUR — Tokenized Euro (tEUR) Repository

Repository met smart contracts, API-gateway, dashboard en tools voor het Tokenized Euro project.

Snelstart

- API-tests uitvoeren:
  ```bash
  cd api
  npm ci
  npx vitest
  ```
- Reconciliatie referentie starten:
  ```bash
  cd tools/reconciliation-ref
  node index.js
  ```

Zie het hoofd-README en `docs/` voor uitgebreide instructies.
