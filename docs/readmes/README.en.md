# tEUR — Tokenized Euro (tEUR) Repository

Repository containing smart contracts, API gateway, dashboard, and tools for the Tokenized Euro project.

Quick start

- Run API tests:
  ```bash
  cd api
  npm ci
  npx vitest
  ```
- Run reconciliation reference:
  ```bash
  cd tools/reconciliation-ref
  node index.js
  ```

See repository README and `docs/` for detailed instructions.
